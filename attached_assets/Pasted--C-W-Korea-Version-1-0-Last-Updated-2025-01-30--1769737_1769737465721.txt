# 📚 C&W Korea 온보딩 시스템 - 매뉴얼 라이브러리 기능 명세서

> **Version:** 1.0  
> **Last Updated:** 2025-01-30  
> **Platform:** Mobile-First Web Application (Replit)

---

## 📋 목차

1. [개요](#1-개요)
2. [사용자 스토리](#2-사용자-스토리)
3. [시스템 아키텍처](#3-시스템-아키텍처)
4. [User Side 기능 상세](#4-user-side-기능-상세)
5. [Admin Side 기능 상세](#5-admin-side-기능-상세)
6. [데이터베이스 스키마](#6-데이터베이스-스키마)
7. [UI/UX 설계](#7-uiux-설계)
8. [API 명세](#8-api-명세)
9. [PDF 뷰어 통합](#9-pdf-뷰어-통합)
10. [검색 시스템](#10-검색-시스템)
11. [모바일 최적화 가이드라인](#11-모바일-최적화-가이드라인)
12. [구현 우선순위](#12-구현-우선순위)

---

## 1. 개요

### 1.1 프로젝트 배경
현재 온보딩 시스템에서 신입사원들이 필요한 매뉴얼(업무 가이드, 시스템 사용법 등)에 쉽게 접근할 수 있는 중앙화된 라이브러리가 필요합니다.

### 1.2 목표
- 부서별로 분류된 매뉴얼 라이브러리 구축
- PDF 파일 업로드 및 인앱 뷰어 제공
- 해시태그 기반 검색 시스템 구현
- 모바일 환경에 최적화된 UX 제공

### 1.3 핵심 기능 요약

| 구분 | 기능 | 설명 |
|------|------|------|
| **User** | 매뉴얼 열람 | 부서별 매뉴얼 목록 확인 및 PDF 뷰어로 열람 |
| **User** | 검색 | 제목, 해시태그, 부서별 검색 |
| **Admin** | 부서 관리 | 부서 생성/수정/삭제 |
| **Admin** | 매뉴얼 관리 | PDF 업로드, 해시태그 설정, 부서 지정 |

### 1.4 기본 부서 구성
- **HR** (Human Resources) - 인사팀
- **WPR** (Workplace Resources) - 사업장자원팀  
- **Finance** - 재무팀

---

## 2. 사용자 스토리

### 2.1 일반 사용자 (신입사원)

```
AS A 신입사원
I WANT TO 왼쪽 상단 메뉴에서 매뉴얼 라이브러리에 접근하고 싶다
SO THAT 필요한 업무 매뉴얼을 쉽게 찾아볼 수 있다
```

```
AS A 신입사원
I WANT TO 부서별로 분류된 매뉴얼을 보고 싶다
SO THAT 내 부서에 관련된 매뉴얼만 빠르게 찾을 수 있다
```

```
AS A 신입사원
I WANT TO 검색어나 해시태그로 매뉴얼을 검색하고 싶다
SO THAT 특정 주제의 매뉴얼을 빠르게 찾을 수 있다
```

```
AS A 신입사원
I WANT TO 앱 내에서 바로 PDF를 열람하고 싶다
SO THAT 별도의 앱 없이 매뉴얼을 볼 수 있다
```

### 2.2 관리자 (Admin)

```
AS AN Admin
I WANT TO 새로운 부서를 생성하고 싶다
SO THAT 조직 변경에 맞게 매뉴얼을 분류할 수 있다
```

```
AS AN Admin
I WANT TO PDF 매뉴얼을 업로드하고 해시태그를 설정하고 싶다
SO THAT 사용자들이 쉽게 검색할 수 있다
```

```
AS AN Admin
I WANT TO 매뉴얼의 부서 지정을 변경하고 싶다
SO THAT 매뉴얼을 적절한 카테고리에 분류할 수 있다
```

---

## 3. 시스템 아키텍처

### 3.1 전체 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (React/Next.js)                  │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   메뉴 탭    │  │  매뉴얼 목록  │  │  PDF 뷰어    │       │
│  │  (Sidebar)   │  │   (Grid)     │  │  (Viewer)    │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│  ┌──────────────┐  ┌──────────────┐                         │
│  │   검색 바    │  │  부서 필터   │                         │
│  └──────────────┘  └──────────────┘                         │
├─────────────────────────────────────────────────────────────┤
│                      Backend API (Node.js)                   │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ 매뉴얼 CRUD  │  │  부서 관리   │  │  검색 엔진   │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
├─────────────────────────────────────────────────────────────┤
│                     Database & Storage                       │
│  ┌──────────────┐  ┌──────────────┐                         │
│  │   SQLite/    │  │  File Storage│                         │
│  │   PostgreSQL │  │  (PDF Files) │                         │
│  └──────────────┘  └──────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 파일 저장 방식

**Option A: 외부 호스팅 (권장)**
- Cloudinary, AWS S3, 또는 Firebase Storage 사용
- PDF URL을 데이터베이스에 저장

**Option B: 로컬 저장**
- Replit의 `/uploads` 폴더에 저장
- 파일 크기 제한 고려 필요

---

## 4. User Side 기능 상세

### 4.1 메뉴 탭 (햄버거 메뉴)

#### 4.1.1 위치 및 트리거
- **위치:** 좌측 상단 "안녕하세요, 김경민님!" 옆
- **아이콘:** 햄버거 메뉴 (☰) 또는 그리드 아이콘
- **동작:** 탭 시 슬라이드 메뉴 오픈

#### 4.1.2 메뉴 구조

```
┌─────────────────────────────────┐
│  ☰  C&W Korea                 X │
├─────────────────────────────────┤
│                                 │
│  🏠  대시보드                    │
│                                 │
│  📚  매뉴얼 라이브러리    →      │
│                                 │
│  ─────────────────────────      │
│                                 │
│  👤  내 정보                     │
│                                 │
│  ⚙️  설정                        │
│                                 │
│  🚪  로그아웃                    │
│                                 │
└─────────────────────────────────┘
```

#### 4.1.3 슬라이드 애니메이션

```css
/* 메뉴 슬라이드 애니메이션 */
.sidebar-menu {
  transform: translateX(-100%);
  transition: transform 0.3s ease-in-out;
}

.sidebar-menu.open {
  transform: translateX(0);
}

/* 배경 오버레이 */
.overlay {
  opacity: 0;
  transition: opacity 0.3s ease-in-out;
}

.overlay.visible {
  opacity: 0.5;
  background: rgba(0, 0, 0, 0.5);
}
```

### 4.2 매뉴얼 라이브러리 메인 화면

#### 4.2.1 화면 구성

```
┌─────────────────────────────────┐
│  ←  매뉴얼 라이브러리            │
├─────────────────────────────────┤
│  ┌─────────────────────────┐    │
│  │ 🔍 매뉴얼 검색...        │    │
│  └─────────────────────────┘    │
├─────────────────────────────────┤
│  부서 필터                       │
│  ┌─────┐ ┌─────┐ ┌─────────┐   │
│  │ 전체 │ │ HR  │ │ WPR    │   │
│  └─────┘ └─────┘ └─────────┘   │
│  ┌─────────┐                    │
│  │ Finance │                    │
│  └─────────┘                    │
├─────────────────────────────────┤
│                                 │
│  📁 HR (3)                      │
│  ├─────────────────────────────┤
│  │ 📄 신입사원 온보딩 가이드     │
│  │    #온보딩 #신입 #필수       │
│  ├─────────────────────────────┤
│  │ 📄 휴가 신청 매뉴얼          │
│  │    #휴가 #근태              │
│  ├─────────────────────────────┤
│  │ 📄 복리후생 안내             │
│  │    #복지 #혜택              │
│  │                             │
│  📁 WPR (2)                     │
│  ├─────────────────────────────┤
│  │ 📄 사무실 이용 가이드        │
│  │    #사무실 #시설            │
│  ├─────────────────────────────┤
│  │ 📄 회의실 예약 방법          │
│  │    #회의실 #예약            │
│                                 │
└─────────────────────────────────┘
```

#### 4.2.2 부서 필터 (Chip/Tag 스타일)

```jsx
// 부서 필터 컴포넌트
const DepartmentFilter = ({ departments, selected, onSelect }) => {
  return (
    <div className="flex flex-wrap gap-2 p-4">
      <Chip 
        label="전체" 
        active={selected === 'all'}
        onClick={() => onSelect('all')}
      />
      {departments.map(dept => (
        <Chip
          key={dept.id}
          label={dept.name}
          active={selected === dept.id}
          onClick={() => onSelect(dept.id)}
          color={dept.color}
        />
      ))}
    </div>
  );
};
```

#### 4.2.3 매뉴얼 카드 컴포넌트

```jsx
// 매뉴얼 카드 (모바일 최적화)
const ManualCard = ({ manual, onClick }) => {
  return (
    <div 
      className="bg-white rounded-lg p-4 shadow-sm border border-gray-100 
                 active:bg-gray-50 transition-colors"
      onClick={() => onClick(manual)}
    >
      <div className="flex items-start gap-3">
        {/* PDF 아이콘 */}
        <div className="w-10 h-10 bg-red-100 rounded-lg flex items-center 
                        justify-center flex-shrink-0">
          <svg className="w-6 h-6 text-red-600">
            {/* PDF Icon */}
          </svg>
        </div>
        
        {/* 내용 */}
        <div className="flex-1 min-w-0">
          <h3 className="font-medium text-gray-900 truncate">
            {manual.title}
          </h3>
          <p className="text-sm text-gray-500 mt-1">
            {manual.department.name}
          </p>
          
          {/* 해시태그 */}
          <div className="flex flex-wrap gap-1 mt-2">
            {manual.hashtags.slice(0, 3).map(tag => (
              <span 
                key={tag}
                className="text-xs bg-blue-50 text-blue-600 px-2 py-0.5 rounded"
              >
                #{tag}
              </span>
            ))}
            {manual.hashtags.length > 3 && (
              <span className="text-xs text-gray-400">
                +{manual.hashtags.length - 3}
              </span>
            )}
          </div>
        </div>
        
        {/* 화살표 */}
        <svg className="w-5 h-5 text-gray-400 flex-shrink-0">
          {/* Chevron Right */}
        </svg>
      </div>
    </div>
  );
};
```

### 4.3 검색 기능

#### 4.3.1 검색 UI

```
┌─────────────────────────────────┐
│  🔍 │ 휴가 신청                  │
├─────────────────────────────────┤
│  최근 검색                       │
│  ┌──────┐ ┌──────┐ ┌──────┐    │
│  │ 출장 │ │ 경비 │ │ 보고서│    │
│  └──────┘ └──────┘ └──────┘    │
├─────────────────────────────────┤
│  인기 태그                       │
│  ┌────────┐ ┌──────┐ ┌──────┐  │
│  │ #온보딩 │ │ #필수 │ │ #신청 │  │
│  └────────┘ └──────┘ └──────┘  │
├─────────────────────────────────┤
│  검색 결과 (2건)                 │
│                                 │
│  📄 휴가 신청 매뉴얼             │
│     HR · #휴가 #신청 #근태       │
│                                 │
│  📄 출장/휴가 정산 가이드        │
│     Finance · #휴가 #출장 #정산  │
│                                 │
└─────────────────────────────────┘
```

#### 4.3.2 검색 로직

```javascript
// 검색 함수
const searchManuals = (query, filters = {}) => {
  const { departmentId, hashtags } = filters;
  
  return manuals.filter(manual => {
    // 부서 필터
    if (departmentId && manual.departmentId !== departmentId) {
      return false;
    }
    
    // 해시태그 필터
    if (hashtags?.length > 0) {
      const hasMatchingTag = hashtags.some(tag => 
        manual.hashtags.includes(tag)
      );
      if (!hasMatchingTag) return false;
    }
    
    // 검색어 매칭 (제목 + 해시태그)
    if (query) {
      const searchLower = query.toLowerCase();
      const titleMatch = manual.title.toLowerCase().includes(searchLower);
      const tagMatch = manual.hashtags.some(tag => 
        tag.toLowerCase().includes(searchLower)
      );
      const deptMatch = manual.department.name.toLowerCase().includes(searchLower);
      
      return titleMatch || tagMatch || deptMatch;
    }
    
    return true;
  });
};
```

### 4.4 PDF 뷰어 화면

#### 4.4.1 뷰어 UI

```
┌─────────────────────────────────┐
│  ←  신입사원 온보딩 가이드     ⋮ │
├─────────────────────────────────┤
│  HR · 2024.01.15 업로드          │
│  #온보딩 #신입 #필수             │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐    │
│  │                         │    │
│  │                         │    │
│  │      PDF 문서 영역       │    │
│  │                         │    │
│  │                         │    │
│  │                         │    │
│  │                         │    │
│  │                         │    │
│  └─────────────────────────┘    │
│                                 │
├─────────────────────────────────┤
│  ◀ │ 1 / 12 │ ▶      🔍 📥     │
└─────────────────────────────────┘
```

#### 4.4.2 PDF 뷰어 기능

| 기능 | 설명 | 제스처/버튼 |
|------|------|------------|
| 페이지 이동 | 이전/다음 페이지 | 좌우 스와이프 또는 버튼 |
| 확대/축소 | 핀치 줌 | 두 손가락 핀치 |
| 전체화면 | 몰입 모드 | 더블 탭 |
| 다운로드 | PDF 저장 | 📥 버튼 |
| 공유 | 링크 공유 | ⋮ 메뉴 > 공유 |

---

## 5. Admin Side 기능 상세

### 5.1 Admin 매뉴얼 관리 탭 추가

기존 Admin 화면의 탭에 "매뉴얼 관리" 추가:

```
┌───────────────┬───────────────┬───────────────┐
│  👥 사용자 관리 │  📋 콘텐츠 관리 │  📚 매뉴얼 관리 │
└───────────────┴───────────────┴───────────────┘
```

### 5.2 매뉴얼 관리 메인 화면

```
┌─────────────────────────────────────────────────┐
│  📚 매뉴얼 관리                                  │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─────────────────┐  ┌─────────────────┐       │
│  │  + 부서 추가     │  │  + 매뉴얼 추가   │       │
│  └─────────────────┘  └─────────────────┘       │
│                                                 │
├─────────────────────────────────────────────────┤
│  부서 목록                                       │
│  ┌─────────────────────────────────────────┐    │
│  │ 🏷️ HR           │ 3개 │  ✏️  │  🗑️  │    │
│  ├─────────────────────────────────────────┤    │
│  │ 🏷️ WPR          │ 2개 │  ✏️  │  🗑️  │    │
│  ├─────────────────────────────────────────┤    │
│  │ 🏷️ Finance      │ 4개 │  ✏️  │  🗑️  │    │
│  └─────────────────────────────────────────┘    │
│                                                 │
├─────────────────────────────────────────────────┤
│  매뉴얼 목록                          🔍 검색    │
│  ┌─────────────────────────────────────────┐    │
│  │ 📄 신입사원 온보딩 가이드                │    │
│  │    HR · #온보딩 #신입       ✏️  │  🗑️  │    │
│  ├─────────────────────────────────────────┤    │
│  │ 📄 휴가 신청 매뉴얼                      │    │
│  │    HR · #휴가 #근태         ✏️  │  🗑️  │    │
│  └─────────────────────────────────────────┘    │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 5.3 부서 추가/수정 모달

```
┌─────────────────────────────────────┐
│  + 새 부서 추가                    X │
├─────────────────────────────────────┤
│                                     │
│  부서명 *                           │
│  ┌─────────────────────────────┐    │
│  │ IT                          │    │
│  └─────────────────────────────┘    │
│                                     │
│  부서 설명                          │
│  ┌─────────────────────────────┐    │
│  │ 정보기술팀                   │    │
│  └─────────────────────────────┘    │
│                                     │
│  테마 색상                          │
│  ○ 🔵  ● 🟢  ○ 🟡  ○ 🟣  ○ 🔴     │
│                                     │
├─────────────────────────────────────┤
│  ┌───────────┐  ┌───────────┐       │
│  │   취소    │  │    저장    │       │
│  └───────────┘  └───────────┘       │
└─────────────────────────────────────┘
```

### 5.4 매뉴얼 추가 화면 (전체 화면)

```
┌─────────────────────────────────────┐
│  ←  새 매뉴얼 추가                   │
├─────────────────────────────────────┤
│                                     │
│  매뉴얼 제목 *                       │
│  ┌─────────────────────────────┐    │
│  │ 출장 정산 가이드             │    │
│  └─────────────────────────────┘    │
│                                     │
│  소속 부서 *                        │
│  ┌─────────────────────────────┐    │
│  │ Finance                   ▼ │    │
│  └─────────────────────────────┘    │
│                                     │
│  해시태그 (쉼표로 구분)              │
│  ┌─────────────────────────────┐    │
│  │ 출장, 정산, 경비, 보고서     │    │
│  └─────────────────────────────┘    │
│  미리보기:                          │
│  #출장  #정산  #경비  #보고서        │
│                                     │
│  PDF 파일 *                         │
│  ┌─────────────────────────────┐    │
│  │                             │    │
│  │     📤 파일을 선택하세요      │    │
│  │                             │    │
│  │    또는 여기에 드래그         │    │
│  │                             │    │
│  └─────────────────────────────┘    │
│  * PDF 파일만 업로드 가능 (최대 10MB)│
│                                     │
│  ┌─────────────────────────────┐    │
│  │ 📄 출장정산가이드_v2.pdf     │ X │
│  │    2.3 MB                   │    │
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │         매뉴얼 등록          │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

### 5.5 해시태그 입력 컴포넌트

```jsx
// 해시태그 입력 컴포넌트
const HashtagInput = ({ value, onChange }) => {
  const [inputValue, setInputValue] = useState('');
  const [tags, setTags] = useState(value || []);

  const handleKeyDown = (e) => {
    if (e.key === 'Enter' || e.key === ',') {
      e.preventDefault();
      addTag();
    }
    if (e.key === 'Backspace' && !inputValue && tags.length > 0) {
      removeTag(tags.length - 1);
    }
  };

  const addTag = () => {
    const tag = inputValue.trim().replace(/^#/, '');
    if (tag && !tags.includes(tag)) {
      const newTags = [...tags, tag];
      setTags(newTags);
      onChange(newTags);
    }
    setInputValue('');
  };

  const removeTag = (index) => {
    const newTags = tags.filter((_, i) => i !== index);
    setTags(newTags);
    onChange(newTags);
  };

  return (
    <div className="border rounded-lg p-2 focus-within:ring-2 focus-within:ring-blue-500">
      <div className="flex flex-wrap gap-2 mb-2">
        {tags.map((tag, index) => (
          <span 
            key={index}
            className="bg-blue-100 text-blue-700 px-2 py-1 rounded-full 
                       text-sm flex items-center gap-1"
          >
            #{tag}
            <button 
              onClick={() => removeTag(index)}
              className="hover:text-blue-900"
            >
              ×
            </button>
          </span>
        ))}
      </div>
      <input
        type="text"
        value={inputValue}
        onChange={(e) => setInputValue(e.target.value)}
        onKeyDown={handleKeyDown}
        onBlur={addTag}
        placeholder="태그 입력 후 Enter"
        className="w-full outline-none text-sm"
      />
    </div>
  );
};
```

### 5.6 매뉴얼 수정 화면

매뉴얼 추가 화면과 동일한 레이아웃, 기존 데이터 pre-fill

```
┌─────────────────────────────────────┐
│  ←  매뉴얼 수정                      │
├─────────────────────────────────────┤
│                                     │
│  매뉴얼 제목 *                       │
│  ┌─────────────────────────────┐    │
│  │ 출장 정산 가이드             │    │ ← 기존 값
│  └─────────────────────────────┘    │
│                                     │
│  ... (동일)                         │
│                                     │
│  현재 파일                          │
│  ┌─────────────────────────────┐    │
│  │ 📄 출장정산가이드_v2.pdf     │    │
│  │    2.3 MB · 2024.01.15 업로드 │    │
│  └─────────────────────────────┘    │
│                                     │
│  ☐ 새 파일로 교체                   │
│                                     │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │          변경사항 저장        │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

---

## 6. 데이터베이스 스키마

### 6.1 테이블 구조

```sql
-- 부서 테이블
CREATE TABLE departments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(100) NOT NULL UNIQUE,
    description TEXT,
    color VARCHAR(7) DEFAULT '#3B82F6',  -- HEX 색상
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_active BOOLEAN DEFAULT TRUE
);

-- 기본 부서 데이터
INSERT INTO departments (name, description, color) VALUES
    ('HR', '인사팀 - Human Resources', '#10B981'),
    ('WPR', '사업장자원팀 - Workplace Resources', '#6366F1'),
    ('Finance', '재무팀', '#F59E0B');

-- 매뉴얼 테이블
CREATE TABLE manuals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title VARCHAR(255) NOT NULL,
    department_id INTEGER NOT NULL,
    file_url TEXT NOT NULL,           -- PDF 파일 URL
    file_name VARCHAR(255),           -- 원본 파일명
    file_size INTEGER,                -- 파일 크기 (bytes)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by INTEGER,               -- Admin 사용자 ID
    is_active BOOLEAN DEFAULT TRUE,
    view_count INTEGER DEFAULT 0,     -- 조회수
    
    FOREIGN KEY (department_id) REFERENCES departments(id)
);

-- 해시태그 테이블
CREATE TABLE hashtags (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(100) NOT NULL UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 매뉴얼-해시태그 연결 테이블 (N:M 관계)
CREATE TABLE manual_hashtags (
    manual_id INTEGER NOT NULL,
    hashtag_id INTEGER NOT NULL,
    
    PRIMARY KEY (manual_id, hashtag_id),
    FOREIGN KEY (manual_id) REFERENCES manuals(id) ON DELETE CASCADE,
    FOREIGN KEY (hashtag_id) REFERENCES hashtags(id) ON DELETE CASCADE
);

-- 검색 기록 테이블 (선택적)
CREATE TABLE search_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    query VARCHAR(255),
    searched_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 인덱스 생성 (검색 성능 향상)
CREATE INDEX idx_manuals_department ON manuals(department_id);
CREATE INDEX idx_manuals_title ON manuals(title);
CREATE INDEX idx_hashtags_name ON hashtags(name);
```

### 6.2 데이터 모델 (TypeScript)

```typescript
// types/manual.ts

interface Department {
  id: number;
  name: string;
  description?: string;
  color: string;
  createdAt: Date;
  updatedAt: Date;
  isActive: boolean;
  manualCount?: number;  // 집계용
}

interface Hashtag {
  id: number;
  name: string;
  createdAt: Date;
}

interface Manual {
  id: number;
  title: string;
  departmentId: number;
  department?: Department;
  fileUrl: string;
  fileName?: string;
  fileSize?: number;
  hashtags: string[];  // 태그명 배열
  createdAt: Date;
  updatedAt: Date;
  createdBy?: number;
  isActive: boolean;
  viewCount: number;
}

interface ManualCreateInput {
  title: string;
  departmentId: number;
  file: File;
  hashtags: string[];
}

interface ManualUpdateInput {
  title?: string;
  departmentId?: number;
  file?: File;
  hashtags?: string[];
}

interface SearchParams {
  query?: string;
  departmentId?: number;
  hashtags?: string[];
  page?: number;
  limit?: number;
}
```

---

## 7. UI/UX 설계

### 7.1 디자인 시스템

#### 7.1.1 색상 팔레트

```css
:root {
  /* Primary - C&W 브랜드 */
  --primary-50: #EFF6FF;
  --primary-100: #DBEAFE;
  --primary-500: #3B82F6;
  --primary-600: #2563EB;
  --primary-700: #1D4ED8;
  
  /* 부서별 색상 */
  --dept-hr: #10B981;        /* Green */
  --dept-wpr: #6366F1;       /* Indigo */
  --dept-finance: #F59E0B;   /* Amber */
  
  /* Neutral */
  --gray-50: #F9FAFB;
  --gray-100: #F3F4F6;
  --gray-200: #E5E7EB;
  --gray-500: #6B7280;
  --gray-700: #374151;
  --gray-900: #111827;
  
  /* Semantic */
  --success: #10B981;
  --warning: #F59E0B;
  --error: #EF4444;
  --info: #3B82F6;
}
```

#### 7.1.2 타이포그래피

```css
/* 모바일 최적화 폰트 크기 */
.text-title {
  font-size: 1.25rem;   /* 20px */
  font-weight: 600;
  line-height: 1.4;
}

.text-subtitle {
  font-size: 1rem;      /* 16px */
  font-weight: 500;
  line-height: 1.5;
}

.text-body {
  font-size: 0.875rem;  /* 14px */
  font-weight: 400;
  line-height: 1.5;
}

.text-caption {
  font-size: 0.75rem;   /* 12px */
  font-weight: 400;
  line-height: 1.4;
  color: var(--gray-500);
}
```

#### 7.1.3 간격 시스템

```css
/* 8px 기반 간격 시스템 */
--spacing-1: 0.25rem;   /* 4px */
--spacing-2: 0.5rem;    /* 8px */
--spacing-3: 0.75rem;   /* 12px */
--spacing-4: 1rem;      /* 16px */
--spacing-5: 1.25rem;   /* 20px */
--spacing-6: 1.5rem;    /* 24px */
--spacing-8: 2rem;      /* 32px */
```

### 7.2 컴포넌트 라이브러리

#### 7.2.1 버튼 스타일

```jsx
// 버튼 컴포넌트
const Button = ({ variant = 'primary', size = 'md', children, ...props }) => {
  const baseStyles = `
    inline-flex items-center justify-center font-medium rounded-lg
    transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2
    disabled:opacity-50 disabled:cursor-not-allowed
  `;
  
  const variants = {
    primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500',
    secondary: 'bg-gray-100 text-gray-700 hover:bg-gray-200 focus:ring-gray-500',
    outline: 'border border-gray-300 text-gray-700 hover:bg-gray-50 focus:ring-gray-500',
    danger: 'bg-red-600 text-white hover:bg-red-700 focus:ring-red-500',
  };
  
  const sizes = {
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-4 py-2 text-sm',
    lg: 'px-6 py-3 text-base',
  };
  
  return (
    <button 
      className={`${baseStyles} ${variants[variant]} ${sizes[size]}`}
      {...props}
    >
      {children}
    </button>
  );
};
```

#### 7.2.2 카드 컴포넌트

```jsx
// 기본 카드
const Card = ({ children, className = '', onClick }) => {
  return (
    <div 
      className={`
        bg-white rounded-xl shadow-sm border border-gray-100
        ${onClick ? 'cursor-pointer active:bg-gray-50' : ''}
        ${className}
      `}
      onClick={onClick}
    >
      {children}
    </div>
  );
};

// 카드 헤더
Card.Header = ({ children, className = '' }) => (
  <div className={`px-4 py-3 border-b border-gray-100 ${className}`}>
    {children}
  </div>
);

// 카드 바디
Card.Body = ({ children, className = '' }) => (
  <div className={`p-4 ${className}`}>
    {children}
  </div>
);
```

### 7.3 화면 전환 애니메이션

```jsx
// 페이지 전환 애니메이션 (Framer Motion)
import { motion, AnimatePresence } from 'framer-motion';

const pageVariants = {
  initial: { opacity: 0, x: 20 },
  animate: { opacity: 1, x: 0 },
  exit: { opacity: 0, x: -20 },
};

const PageTransition = ({ children }) => (
  <motion.div
    initial="initial"
    animate="animate"
    exit="exit"
    variants={pageVariants}
    transition={{ duration: 0.2 }}
  >
    {children}
  </motion.div>
);
```

---

## 8. API 명세

### 8.1 부서 API

```yaml
# 부서 목록 조회
GET /api/departments
Response:
  - 200: { departments: Department[], total: number }

# 부서 생성 (Admin)
POST /api/departments
Body: { name: string, description?: string, color?: string }
Response:
  - 201: { department: Department }
  - 400: { error: "Department name already exists" }

# 부서 수정 (Admin)
PUT /api/departments/:id
Body: { name?: string, description?: string, color?: string }
Response:
  - 200: { department: Department }
  - 404: { error: "Department not found" }

# 부서 삭제 (Admin)
DELETE /api/departments/:id
Response:
  - 200: { success: true }
  - 400: { error: "Cannot delete department with manuals" }
```

### 8.2 매뉴얼 API

```yaml
# 매뉴얼 목록 조회
GET /api/manuals
Query:
  - departmentId?: number
  - search?: string
  - hashtags?: string (comma-separated)
  - page?: number (default: 1)
  - limit?: number (default: 20)
Response:
  - 200: { 
      manuals: Manual[], 
      total: number,
      page: number,
      totalPages: number 
    }

# 매뉴얼 상세 조회
GET /api/manuals/:id
Response:
  - 200: { manual: Manual }
  - 404: { error: "Manual not found" }

# 매뉴얼 생성 (Admin)
POST /api/manuals
Content-Type: multipart/form-data
Body:
  - title: string
  - departmentId: number
  - hashtags: string (JSON array)
  - file: File (PDF)
Response:
  - 201: { manual: Manual }
  - 400: { error: "Invalid file type" }

# 매뉴얼 수정 (Admin)
PUT /api/manuals/:id
Content-Type: multipart/form-data
Body:
  - title?: string
  - departmentId?: number
  - hashtags?: string (JSON array)
  - file?: File (PDF)
Response:
  - 200: { manual: Manual }
  - 404: { error: "Manual not found" }

# 매뉴얼 삭제 (Admin)
DELETE /api/manuals/:id
Response:
  - 200: { success: true }
  - 404: { error: "Manual not found" }

# 조회수 증가
POST /api/manuals/:id/view
Response:
  - 200: { viewCount: number }
```

### 8.3 검색/해시태그 API

```yaml
# 해시태그 목록 (인기순)
GET /api/hashtags
Query:
  - limit?: number (default: 10)
Response:
  - 200: { hashtags: { name: string, count: number }[] }

# 검색 제안 (자동완성)
GET /api/search/suggestions
Query:
  - q: string
Response:
  - 200: { 
      titles: string[],
      hashtags: string[],
      departments: string[]
    }
```

### 8.4 API 구현 예시 (Node.js/Express)

```javascript
// routes/manuals.js
const express = require('express');
const router = express.Router();
const multer = require('multer');
const { v4: uuidv4 } = require('uuid');

// 파일 업로드 설정
const upload = multer({
  storage: multer.memoryStorage(),
  limits: { fileSize: 10 * 1024 * 1024 }, // 10MB
  fileFilter: (req, file, cb) => {
    if (file.mimetype === 'application/pdf') {
      cb(null, true);
    } else {
      cb(new Error('PDF 파일만 업로드 가능합니다.'), false);
    }
  }
});

// 매뉴얼 목록 조회
router.get('/', async (req, res) => {
  try {
    const { departmentId, search, hashtags, page = 1, limit = 20 } = req.query;
    
    let query = db('manuals')
      .select(
        'manuals.*',
        'departments.name as departmentName',
        'departments.color as departmentColor'
      )
      .leftJoin('departments', 'manuals.department_id', 'departments.id')
      .where('manuals.is_active', true);
    
    // 부서 필터
    if (departmentId) {
      query = query.where('department_id', departmentId);
    }
    
    // 검색어 필터
    if (search) {
      query = query.where((builder) => {
        builder
          .whereILike('manuals.title', `%${search}%`)
          .orWhereExists((subquery) => {
            subquery
              .select('*')
              .from('manual_hashtags')
              .join('hashtags', 'manual_hashtags.hashtag_id', 'hashtags.id')
              .whereRaw('manual_hashtags.manual_id = manuals.id')
              .whereILike('hashtags.name', `%${search}%`);
          });
      });
    }
    
    // 해시태그 필터
    if (hashtags) {
      const tagList = hashtags.split(',').map(t => t.trim());
      query = query.whereExists((subquery) => {
        subquery
          .select('*')
          .from('manual_hashtags')
          .join('hashtags', 'manual_hashtags.hashtag_id', 'hashtags.id')
          .whereRaw('manual_hashtags.manual_id = manuals.id')
          .whereIn('hashtags.name', tagList);
      });
    }
    
    // 페이지네이션
    const offset = (page - 1) * limit;
    const [{ count }] = await query.clone().count('* as count');
    const manuals = await query
      .orderBy('manuals.created_at', 'desc')
      .offset(offset)
      .limit(limit);
    
    // 해시태그 조회
    const manualsWithTags = await Promise.all(
      manuals.map(async (manual) => {
        const tags = await db('manual_hashtags')
          .select('hashtags.name')
          .join('hashtags', 'manual_hashtags.hashtag_id', 'hashtags.id')
          .where('manual_id', manual.id);
        
        return {
          ...manual,
          hashtags: tags.map(t => t.name)
        };
      })
    );
    
    res.json({
      manuals: manualsWithTags,
      total: parseInt(count),
      page: parseInt(page),
      totalPages: Math.ceil(count / limit)
    });
  } catch (error) {
    console.error('매뉴얼 조회 오류:', error);
    res.status(500).json({ error: '서버 오류가 발생했습니다.' });
  }
});

// 매뉴얼 생성
router.post('/', upload.single('file'), async (req, res) => {
  try {
    const { title, departmentId, hashtags } = req.body;
    const file = req.file;
    
    if (!file) {
      return res.status(400).json({ error: 'PDF 파일이 필요합니다.' });
    }
    
    // 파일 업로드 (Cloudinary 예시)
    const uploadResult = await cloudinary.uploader.upload_stream(
      { 
        resource_type: 'raw',
        folder: 'manuals',
        public_id: `${uuidv4()}`
      },
      (error, result) => {
        if (error) throw error;
        return result;
      }
    ).end(file.buffer);
    
    // 매뉴얼 저장
    const [manualId] = await db('manuals').insert({
      title,
      department_id: departmentId,
      file_url: uploadResult.secure_url,
      file_name: file.originalname,
      file_size: file.size,
      created_by: req.user.id
    });
    
    // 해시태그 처리
    const tagList = JSON.parse(hashtags || '[]');
    for (const tagName of tagList) {
      // 해시태그 생성 또는 조회
      let [hashtag] = await db('hashtags').where('name', tagName);
      if (!hashtag) {
        const [hashtagId] = await db('hashtags').insert({ name: tagName });
        hashtag = { id: hashtagId };
      }
      
      // 연결
      await db('manual_hashtags').insert({
        manual_id: manualId,
        hashtag_id: hashtag.id
      });
    }
    
    const manual = await getManualById(manualId);
    res.status(201).json({ manual });
  } catch (error) {
    console.error('매뉴얼 생성 오류:', error);
    res.status(500).json({ error: '서버 오류가 발생했습니다.' });
  }
});

module.exports = router;
```

---

## 9. PDF 뷰어 통합

### 9.1 라이브러리 선택

**추천: react-pdf**

```bash
npm install react-pdf
```

### 9.2 PDF 뷰어 컴포넌트

```jsx
// components/PDFViewer.jsx
import { useState, useCallback } from 'react';
import { Document, Page, pdfjs } from 'react-pdf';
import 'react-pdf/dist/esm/Page/AnnotationLayer.css';
import 'react-pdf/dist/esm/Page/TextLayer.css';

// PDF.js 워커 설정
pdfjs.GlobalWorkerOptions.workerSrc = `//cdnjs.cloudflare.com/ajax/libs/pdf.js/${pdfjs.version}/pdf.worker.min.js`;

const PDFViewer = ({ url, title, onClose }) => {
  const [numPages, setNumPages] = useState(null);
  const [pageNumber, setPageNumber] = useState(1);
  const [scale, setScale] = useState(1);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  const onDocumentLoadSuccess = ({ numPages }) => {
    setNumPages(numPages);
    setLoading(false);
  };

  const onDocumentLoadError = (error) => {
    console.error('PDF 로드 오류:', error);
    setError('PDF를 불러올 수 없습니다.');
    setLoading(false);
  };

  const goToPrevPage = () => {
    setPageNumber(prev => Math.max(prev - 1, 1));
  };

  const goToNextPage = () => {
    setPageNumber(prev => Math.min(prev + 1, numPages));
  };

  const zoomIn = () => {
    setScale(prev => Math.min(prev + 0.25, 3));
  };

  const zoomOut = () => {
    setScale(prev => Math.max(prev - 0.25, 0.5));
  };

  const downloadPDF = () => {
    const link = document.createElement('a');
    link.href = url;
    link.download = title || 'manual.pdf';
    link.click();
  };

  // 스와이프 제스처 처리
  const [touchStart, setTouchStart] = useState(null);
  
  const handleTouchStart = (e) => {
    setTouchStart(e.touches[0].clientX);
  };
  
  const handleTouchEnd = (e) => {
    if (!touchStart) return;
    
    const touchEnd = e.changedTouches[0].clientX;
    const diff = touchStart - touchEnd;
    
    if (Math.abs(diff) > 50) {
      if (diff > 0) {
        goToNextPage();
      } else {
        goToPrevPage();
      }
    }
    
    setTouchStart(null);
  };

  return (
    <div className="fixed inset-0 bg-white z-50 flex flex-col">
      {/* 헤더 */}
      <header className="flex items-center justify-between px-4 py-3 border-b bg-white">
        <button 
          onClick={onClose}
          className="p-2 -ml-2 hover:bg-gray-100 rounded-full"
        >
          <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />
          </svg>
        </button>
        <h1 className="flex-1 text-center font-medium truncate px-2">
          {title}
        </h1>
        <button 
          onClick={() => {/* 더보기 메뉴 */}}
          className="p-2 -mr-2 hover:bg-gray-100 rounded-full"
        >
          <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 5v.01M12 12v.01M12 19v.01" />
          </svg>
        </button>
      </header>

      {/* PDF 뷰어 영역 */}
      <div 
        className="flex-1 overflow-auto bg-gray-100"
        onTouchStart={handleTouchStart}
        onTouchEnd={handleTouchEnd}
      >
        {loading && (
          <div className="flex items-center justify-center h-full">
            <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600" />
          </div>
        )}
        
        {error && (
          <div className="flex flex-col items-center justify-center h-full text-gray-500">
            <svg className="w-16 h-16 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
            <p>{error}</p>
          </div>
        )}
        
        <Document
          file={url}
          onLoadSuccess={onDocumentLoadSuccess}
          onLoadError={onDocumentLoadError}
          loading={null}
          className="flex justify-center py-4"
        >
          <Page 
            pageNumber={pageNumber} 
            scale={scale}
            className="shadow-lg"
            renderTextLayer={true}
            renderAnnotationLayer={true}
          />
        </Document>
      </div>

      {/* 하단 컨트롤 바 */}
      <footer className="flex items-center justify-between px-4 py-3 border-t bg-white">
        {/* 페이지 네비게이션 */}
        <div className="flex items-center gap-2">
          <button 
            onClick={goToPrevPage}
            disabled={pageNumber <= 1}
            className="p-2 hover:bg-gray-100 rounded-full disabled:opacity-30"
          >
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />
            </svg>
          </button>
          <span className="text-sm text-gray-600 min-w-[60px] text-center">
            {pageNumber} / {numPages || '-'}
          </span>
          <button 
            onClick={goToNextPage}
            disabled={pageNumber >= numPages}
            className="p-2 hover:bg-gray-100 rounded-full disabled:opacity-30"
          >
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
            </svg>
          </button>
        </div>

        {/* 줌 & 다운로드 */}
        <div className="flex items-center gap-2">
          <button 
            onClick={zoomOut}
            disabled={scale <= 0.5}
            className="p-2 hover:bg-gray-100 rounded-full disabled:opacity-30"
          >
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0zM13 10H7" />
            </svg>
          </button>
          <span className="text-sm text-gray-600 min-w-[40px] text-center">
            {Math.round(scale * 100)}%
          </span>
          <button 
            onClick={zoomIn}
            disabled={scale >= 3}
            className="p-2 hover:bg-gray-100 rounded-full disabled:opacity-30"
          >
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0zM10 7v6m3-3H7" />
            </svg>
          </button>
          <div className="w-px h-6 bg-gray-200 mx-2" />
          <button 
            onClick={downloadPDF}
            className="p-2 hover:bg-gray-100 rounded-full"
          >
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
            </svg>
          </button>
        </div>
      </footer>
    </div>
  );
};

export default PDFViewer;
```

### 9.3 모바일 최적화 PDF 설정

```jsx
// PDF 페이지 크기 자동 조절
const usePDFScale = (containerRef) => {
  const [scale, setScale] = useState(1);
  
  useEffect(() => {
    const updateScale = () => {
      if (containerRef.current) {
        const containerWidth = containerRef.current.clientWidth;
        const pdfWidth = 612; // A4 기본 너비 (포인트)
        const newScale = (containerWidth - 32) / pdfWidth; // 16px 패딩
        setScale(Math.min(newScale, 1.5));
      }
    };
    
    updateScale();
    window.addEventListener('resize', updateScale);
    return () => window.removeEventListener('resize', updateScale);
  }, []);
  
  return scale;
};
```

---

## 10. 검색 시스템

### 10.1 검색 인덱싱 전략

```javascript
// 검색 최적화를 위한 인덱스 생성
const createSearchIndex = async () => {
  // 제목 검색 인덱스
  await db.raw(`
    CREATE VIRTUAL TABLE IF NOT EXISTS manuals_fts USING fts5(
      title,
      content='manuals',
      content_rowid='id'
    )
  `);
  
  // 트리거: 매뉴얼 추가 시 FTS 업데이트
  await db.raw(`
    CREATE TRIGGER IF NOT EXISTS manuals_ai AFTER INSERT ON manuals BEGIN
      INSERT INTO manuals_fts(rowid, title) VALUES (new.id, new.title);
    END
  `);
};
```

### 10.2 검색 컴포넌트

```jsx
// components/SearchBar.jsx
import { useState, useEffect, useRef } from 'react';
import { useDebounce } from '../hooks/useDebounce';

const SearchBar = ({ onSearch, onSuggestionSelect }) => {
  const [query, setQuery] = useState('');
  const [suggestions, setSuggestions] = useState([]);
  const [showSuggestions, setShowSuggestions] = useState(false);
  const [recentSearches, setRecentSearches] = useState([]);
  const inputRef = useRef(null);
  
  const debouncedQuery = useDebounce(query, 300);

  // 최근 검색어 로드
  useEffect(() => {
    const saved = localStorage.getItem('recentManualSearches');
    if (saved) {
      setRecentSearches(JSON.parse(saved).slice(0, 5));
    }
  }, []);

  // 자동완성 제안 가져오기
  useEffect(() => {
    if (debouncedQuery.length >= 2) {
      fetchSuggestions(debouncedQuery);
    } else {
      setSuggestions([]);
    }
  }, [debouncedQuery]);

  const fetchSuggestions = async (q) => {
    try {
      const res = await fetch(`/api/search/suggestions?q=${encodeURIComponent(q)}`);
      const data = await res.json();
      setSuggestions(data);
    } catch (error) {
      console.error('제안 가져오기 오류:', error);
    }
  };

  const handleSearch = (searchQuery) => {
    const q = searchQuery || query;
    if (!q.trim()) return;
    
    // 최근 검색어 저장
    const newRecent = [q, ...recentSearches.filter(r => r !== q)].slice(0, 5);
    setRecentSearches(newRecent);
    localStorage.setItem('recentManualSearches', JSON.stringify(newRecent));
    
    onSearch(q);
    setShowSuggestions(false);
  };

  const handleSuggestionClick = (suggestion) => {
    setQuery(suggestion);
    handleSearch(suggestion);
    onSuggestionSelect?.(suggestion);
  };

  const clearRecentSearches = () => {
    setRecentSearches([]);
    localStorage.removeItem('recentManualSearches');
  };

  return (
    <div className="relative">
      {/* 검색 입력 */}
      <div className="relative">
        <input
          ref={inputRef}
          type="text"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          onFocus={() => setShowSuggestions(true)}
          onKeyDown={(e) => e.key === 'Enter' && handleSearch()}
          placeholder="매뉴얼 검색..."
          className="w-full pl-10 pr-4 py-3 bg-gray-100 rounded-xl
                     focus:bg-white focus:ring-2 focus:ring-blue-500 
                     focus:outline-none transition-all"
        />
        <svg 
          className="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400"
          fill="none" 
          stroke="currentColor" 
          viewBox="0 0 24 24"
        >
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} 
                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
        
        {query && (
          <button
            onClick={() => setQuery('')}
            className="absolute right-3 top-1/2 -translate-y-1/2 p-1 
                       hover:bg-gray-200 rounded-full"
          >
            <svg className="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        )}
      </div>

      {/* 제안 드롭다운 */}
      {showSuggestions && (
        <div className="absolute top-full left-0 right-0 mt-2 bg-white rounded-xl 
                        shadow-lg border border-gray-200 z-10 max-h-80 overflow-auto">
          
          {/* 최근 검색어 */}
          {!query && recentSearches.length > 0 && (
            <div className="p-3">
              <div className="flex items-center justify-between mb-2">
                <span className="text-xs font-medium text-gray-500">최근 검색</span>
                <button 
                  onClick={clearRecentSearches}
                  className="text-xs text-blue-600"
                >
                  지우기
                </button>
              </div>
              <div className="flex flex-wrap gap-2">
                {recentSearches.map((term, i) => (
                  <button
                    key={i}
                    onClick={() => handleSuggestionClick(term)}
                    className="px-3 py-1.5 bg-gray-100 rounded-full text-sm 
                               hover:bg-gray-200 transition-colors"
                  >
                    {term}
                  </button>
                ))}
              </div>
            </div>
          )}
          
          {/* 검색 제안 */}
          {query && suggestions.titles?.length > 0 && (
            <div className="p-3 border-t">
              <span className="text-xs font-medium text-gray-500 mb-2 block">
                매뉴얼
              </span>
              {suggestions.titles.map((title, i) => (
                <button
                  key={i}
                  onClick={() => handleSuggestionClick(title)}
                  className="w-full text-left px-3 py-2 hover:bg-gray-50 
                             rounded-lg flex items-center gap-2"
                >
                  <svg className="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                  </svg>
                  <span className="truncate">{title}</span>
                </button>
              ))}
            </div>
          )}
          
          {/* 해시태그 제안 */}
          {query && suggestions.hashtags?.length > 0 && (
            <div className="p-3 border-t">
              <span className="text-xs font-medium text-gray-500 mb-2 block">
                태그
              </span>
              <div className="flex flex-wrap gap-2">
                {suggestions.hashtags.map((tag, i) => (
                  <button
                    key={i}
                    onClick={() => handleSuggestionClick(`#${tag}`)}
                    className="px-3 py-1.5 bg-blue-50 text-blue-600 
                               rounded-full text-sm hover:bg-blue-100"
                  >
                    #{tag}
                  </button>
                ))}
              </div>
            </div>
          )}
          
          {query && !suggestions.titles?.length && !suggestions.hashtags?.length && (
            <div className="p-8 text-center text-gray-500">
              <p>검색 결과가 없습니다</p>
            </div>
          )}
        </div>
      )}
    </div>
  );
};

export default SearchBar;
```

---

## 11. 모바일 최적화 가이드라인

### 11.1 터치 타겟 크기

```css
/* 최소 터치 영역: 44x44px (Apple HIG 권장) */
.touch-target {
  min-width: 44px;
  min-height: 44px;
}

/* 버튼 패딩으로 터치 영역 확보 */
.btn-touch {
  padding: 12px 16px;
}
```

### 11.2 Safe Area 처리

```css
/* iOS 노치 및 홈 인디케이터 대응 */
.safe-area-top {
  padding-top: env(safe-area-inset-top);
}

.safe-area-bottom {
  padding-bottom: env(safe-area-inset-bottom);
}

/* 전체 레이아웃 */
.app-container {
  min-height: 100vh;
  min-height: -webkit-fill-available;
  padding: env(safe-area-inset-top) env(safe-area-inset-right) 
           env(safe-area-inset-bottom) env(safe-area-inset-left);
}
```

### 11.3 스크롤 최적화

```css
/* 부드러운 스크롤 */
.scroll-container {
  -webkit-overflow-scrolling: touch;
  overscroll-behavior: contain;
}

/* 스크롤바 숨김 (모바일) */
.hide-scrollbar::-webkit-scrollbar {
  display: none;
}
.hide-scrollbar {
  -ms-overflow-style: none;
  scrollbar-width: none;
}
```

### 11.4 로딩 상태

```jsx
// 스켈레톤 로딩
const ManualCardSkeleton = () => (
  <div className="bg-white rounded-lg p-4 animate-pulse">
    <div className="flex items-start gap-3">
      <div className="w-10 h-10 bg-gray-200 rounded-lg" />
      <div className="flex-1">
        <div className="h-4 bg-gray-200 rounded w-3/4 mb-2" />
        <div className="h-3 bg-gray-200 rounded w-1/4 mb-2" />
        <div className="flex gap-2">
          <div className="h-5 bg-gray-200 rounded w-12" />
          <div className="h-5 bg-gray-200 rounded w-16" />
        </div>
      </div>
    </div>
  </div>
);
```

### 11.5 오프라인 지원 (PWA)

```javascript
// service-worker.js
const CACHE_NAME = 'manual-cache-v1';
const urlsToCache = [
  '/',
  '/manuals',
  '/static/js/main.js',
  '/static/css/main.css',
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then((cache) => cache.addAll(urlsToCache))
  );
});

self.addEventListener('fetch', (event) => {
  // PDF 파일은 네트워크 우선
  if (event.request.url.includes('.pdf')) {
    event.respondWith(
      fetch(event.request).catch(() => caches.match(event.request))
    );
    return;
  }
  
  // 기타 리소스는 캐시 우선
  event.respondWith(
    caches.match(event.request)
      .then((response) => response || fetch(event.request))
  );
});
```

---

## 12. 구현 우선순위

### Phase 1: 기본 구조 (1주)

| 우선순위 | 작업 | 예상 시간 |
|---------|------|----------|
| P0 | 데이터베이스 스키마 생성 | 2시간 |
| P0 | 부서 CRUD API | 4시간 |
| P0 | 매뉴얼 CRUD API | 8시간 |
| P0 | 파일 업로드 연동 | 4시간 |

### Phase 2: User Side UI (1주)

| 우선순위 | 작업 | 예상 시간 |
|---------|------|----------|
| P0 | 사이드 메뉴 구현 | 4시간 |
| P0 | 매뉴얼 라이브러리 목록 화면 | 6시간 |
| P0 | PDF 뷰어 통합 | 8시간 |
| P1 | 부서 필터 | 2시간 |

### Phase 3: 검색 & Admin (1주)

| 우선순위 | 작업 | 예상 시간 |
|---------|------|----------|
| P0 | 기본 검색 기능 | 4시간 |
| P1 | 해시태그 검색 | 4시간 |
| P0 | Admin 매뉴얼 관리 탭 | 8시간 |
| P1 | Admin 부서 관리 | 4시간 |

### Phase 4: 개선 & 최적화 (1주)

| 우선순위 | 작업 | 예상 시간 |
|---------|------|----------|
| P1 | 검색 자동완성 | 4시간 |
| P2 | 최근 검색어 | 2시간 |
| P1 | 로딩 상태 & 에러 처리 | 4시간 |
| P2 | 오프라인 지원 | 6시간 |

---

## 부록: 체크리스트

### 개발 완료 체크리스트

- [ ] 데이터베이스 마이그레이션 완료
- [ ] 부서 CRUD 기능 테스트
- [ ] 매뉴얼 업로드/다운로드 테스트
- [ ] PDF 뷰어 모바일 테스트
- [ ] 검색 기능 테스트
- [ ] Admin 권한 체크
- [ ] 에러 핸들링 검증
- [ ] 성능 테스트 (대용량 PDF)

### QA 체크리스트

- [ ] iOS Safari 테스트
- [ ] Android Chrome 테스트
- [ ] 다양한 화면 크기 테스트
- [ ] 네트워크 느린 환경 테스트
- [ ] 접근성 테스트
- [ ] 보안 취약점 검사

---

> **문서 작성:** Claude AI  
> **최종 검토 필요:** 프로젝트 담당자  
> **다음 업데이트 예정:** 개발 진행에 따라 수정